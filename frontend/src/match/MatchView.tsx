/**
 * Live match view (Phase 3): agent panels streaming reasoning, the
 * environment's public_view state, and a running scoreboard. Adapts to
 * whatever shape public_view returns rather than hard-coding one game's
 * layout.
 */

export function MatchView() {
  return (
    <div className="rounded-lg border border-neutral-800 p-6 text-neutral-400">
      Match view — Phase 3.
    </div>
  );
}
