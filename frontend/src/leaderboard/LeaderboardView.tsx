/**
 * Aggregate leaderboard (Phase 4): win rates and mean payoffs by strategy
 * and model, with honest match counts alongside every claim.
 */

export function LeaderboardView() {
  return (
    <div className="rounded-lg border border-neutral-800 p-6 text-neutral-400">
      Leaderboard — Phase 4.
    </div>
  );
}
